import 'package:flutter/Material.dart';

class StationWidget extends StatelessWidget {
  const StationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
